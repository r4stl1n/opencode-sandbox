# Real provider keys live here, only ever loaded into the proxy container.
# The sandbox itself never sees these — opencode inside the sandbox uses a
# placeholder key (e.g. "sandbox") and routes through http://opencode-sandbox-proxy:4000.

# Anthropic — used for the proxy's /anthropic/* route.
ANTHROPIC_API_KEY=

# OpenAI — used for the proxy's /v1/* route.
OPENAI_API_KEY=

# Generic OpenAI-compatible upstream — used for the proxy's /compat/* route.
# Point this at LM Studio, llama.cpp, OpenRouter, vLLM, etc. Leave blank to
# disable the /compat route entirely.
# (Ignored when USE_LOCAL=1 — the script forces the upstream to the local ollama.)
OPENAI_COMPAT_UPSTREAM=
OPENAI_COMPAT_API_KEY=

# ── LOCAL OLLAMA ────────────────────────────────────────────────────────────
# Set USE_LOCAL=1 to spin up a local Ollama container alongside the proxy.
# The two models below are pulled on first startup and kept in a named volume,
# so subsequent starts are instant. The proxy's /compat route is auto-pointed
# at http://ollama:11434 when USE_LOCAL=1 (overrides OPENAI_COMPAT_UPSTREAM).
USE_LOCAL=1
OLLAMA_MODEL_PRIMARY=qwen3.5:0.8b
OLLAMA_MODEL_SECONDARY=qwen3.5:2b

# Optional: override the upstream base URLs (e.g. for a regional Anthropic
# endpoint or a proxy in front of OpenAI). Defaults shown.
# ANTHROPIC_UPSTREAM=https://api.anthropic.com
# OPENAI_UPSTREAM=https://api.openai.com
